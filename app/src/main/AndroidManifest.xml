<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cassianomoura.testeapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".control.AlarmReceiverExercicios" />
        <activity android:name=".view.EasyCallActivity" />
        <activity android:name=".view.AgendaAcompanhamentoActivity" />
        <activity android:name=".view.AARemediosActivity" />
        <activity android:name=".view.AADiarioActivity" />
        <activity android:name=".view.AAExerciciosActivity" />
        <activity android:name=".view.GravarAtividadeActivity" />
        <activity android:name=".view.GravarRemedioActivity" />
        <activity android:name=".view.GravarExercicioActivity"></activity>
    </application>

</manifest>